# example.tasks.gitpod.yml
# rename to .gitpod.yml before using

tasks:
  - name: Install and run http-webserver using NPM
    init: |
      npm install -y npx     
    command: |
      npx --yes http-server
